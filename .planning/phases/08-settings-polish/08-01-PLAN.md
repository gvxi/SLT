---
phase: 08-settings-polish
plan: 01
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - src/app/settings/page.tsx
  - src/components/shared/ConfirmDialog.tsx
  - src/components/shared/EmptyState.tsx
  - src/components/shared/SearchBar.tsx
  - src/app/error.tsx
  - src/app/not-found.tsx
autonomous: false
must_haves:
  truths:
    - "Settings page allows theme toggle, language preference, profile edit"
    - "All pages have proper empty states"
    - "Error boundaries catch and display errors gracefully"
    - "Confirm dialogs appear before destructive actions"
  artifacts:
    - path: "src/app/settings/page.tsx"
      provides: "Settings page"
    - path: "src/components/shared/EmptyState.tsx"
      provides: "Reusable empty state component"
    - path: "src/app/error.tsx"
      provides: "Error boundary"
  key_links:
    - from: "src/app/settings/page.tsx"
      to: "src/store/uiStore.ts"
      via: "Theme and language toggles"
      pattern: "useUIStore"
---

<objective>
Build settings page and add polish: empty states, error boundaries, confirm dialogs, loading states.

Purpose: Final quality pass — production-ready UX.
Output: Polished app with proper edge case handling.
</objective>

<execution_context>
@C:/Users/SAI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/SAI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@AGENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build settings page and shared utility components</name>
  <files>src/app/settings/page.tsx, src/components/shared/ConfirmDialog.tsx, src/components/shared/EmptyState.tsx, src/components/shared/SearchBar.tsx</files>
  <action>
    Create `src/app/settings/page.tsx` ('use client'):
    - Sections in MUI Cards:
      1. Profile: full_name, avatar_url (upload placeholder), edit + save via useUpdateProfile
      2. Appearance: theme toggle (light/dark) via uiStore.toggleTheme
      3. Language: language selector (EN/AR) via uiStore.setLanguage
    - All labels via t()

    Create `src/components/shared/ConfirmDialog.tsx`:
    - MUI Dialog with title, message, cancel + confirm buttons
    - Confirm button is red for destructive actions
    - Props: open, title, message, onConfirm, onCancel, confirmLabel, variant ('danger' | 'default')
    - Integrate into all delete actions across the app

    Create `src/components/shared/EmptyState.tsx`:
    - Centered illustration area + message + optional action button
    - Props: icon, title, description, actionLabel, onAction
    - Integrate into tasks (no tasks), products (no products), invoices (no invoices), quotations (no quotations)

    Create `src/components/shared/SearchBar.tsx`:
    - MUI TextField with search icon, debounced input (300ms)
    - Props: value, onChange, placeholder
    - Used across list pages
  </action>
  <verify>Settings page renders with theme toggle and language selector. ConfirmDialog shows before deletes. Empty states display on empty lists.</verify>
  <done>Settings page functional. Shared components integrated across all pages.</done>
</task>

<task type="auto">
  <name>Task 2: Add error boundaries, loading states, not-found page</name>
  <files>src/app/error.tsx, src/app/not-found.tsx</files>
  <action>
    Create `src/app/error.tsx` ('use client'):
    - Error boundary component with MUI styled error message
    - "Try again" button that calls reset()
    - Logs error to console

    Create `src/app/not-found.tsx`:
    - 404 page with MUI styling
    - "Go to Dashboard" link
    - Bilingual (uses t())

    Verify all list pages have loading skeletons (MUI Skeleton) while data loads via React Query isLoading state.
  </action>
  <verify>Navigate to /nonexistent — see 404 page. Force an error — error boundary catches it. Loading states visible on slow connections.</verify>
  <done>Error handling, 404, and loading states complete across the app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Settings page, empty states, error handling, confirm dialogs, final polish</what-built>
  <how-to-verify>
    1. Go to /settings — toggle theme (light/dark), change language
    2. Delete a task — confirm dialog appears
    3. Go to /products with no products — see empty state with "Create Product" action
    4. Navigate to /random-url — see 404 page
    5. Full walkthrough: login → dashboard → create task → create product → create invoice → export PDF → create quotation → convert to invoice
    6. Test in Arabic mode end-to-end
    7. Test on mobile viewport
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Settings page works (theme, language, profile)
- Empty states appear on all empty lists
- Error boundary catches errors
- 404 page works
- All delete actions have confirm dialogs
</verification>

<success_criteria>
Production-ready polish: proper empty states, error handling, settings, confirm dialogs, loading states throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/08-settings-polish/08-01-SUMMARY.md`
</output>

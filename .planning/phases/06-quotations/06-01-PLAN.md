---
phase: 06-quotations
plan: 01
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - src/app/quotations/page.tsx
  - src/app/quotations/new/page.tsx
  - src/app/quotations/[id]/page.tsx
  - src/components/documents/QuotationPdf.tsx
  - src/app/api/quotations/[id]/convert/route.ts
autonomous: false
must_haves:
  truths:
    - "Quotation CRUD works like invoices (reuses DocumentForm)"
    - "Convert to Invoice creates a new invoice from quotation data"
    - "Quotation PDF exports correctly"
    - "Quotation status workflow works (draft→sent→accepted/rejected/expired)"
  artifacts:
    - path: "src/app/quotations/page.tsx"
      provides: "Quotation list page"
    - path: "src/app/api/quotations/[id]/convert/route.ts"
      provides: "Convert-to-invoice API endpoint"
    - path: "src/components/documents/QuotationPdf.tsx"
      provides: "Quotation PDF template"
  key_links:
    - from: "src/app/quotations/[id]/page.tsx"
      to: "src/app/api/quotations/[id]/convert/route.ts"
      via: "Convert to Invoice button"
      pattern: "convert"
---

<objective>
Build quotation management: reuse invoice components, add convert-to-invoice, quotation PDF.

Purpose: Sales workflow — quotations convert to invoices when accepted.
Output: Working /quotations pages with conversion flow.
</objective>

<execution_context>
@C:/Users/SAI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/SAI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@AGENT.md
@.planning/phases/05-invoices/05-01-SUMMARY.md
@.planning/phases/05-invoices/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build quotation pages reusing document components</name>
  <files>src/app/quotations/page.tsx, src/app/quotations/new/page.tsx, src/app/quotations/[id]/page.tsx</files>
  <action>
    Create `src/app/quotations/page.tsx`:
    - Same structure as invoices list but with quotation statuses (Draft, Sent, Accepted, Rejected, Expired)
    - Uses useQuotations() hook

    Create `src/app/quotations/new/page.tsx`:
    - Uses DocumentForm with type='quotation'
    - Date field is "expiry_date" instead of "due_date"
    - On submit: useCreateQuotation()

    Create `src/app/quotations/[id]/page.tsx`:
    - Uses DocumentForm in edit mode
    - Additional action button: "Convert to Invoice" (only visible when status is 'accepted')
    - On convert: POST /api/quotations/[id]/convert → creates invoice, sets converted_invoice_id, redirects to new invoice
    - PDF export button (like invoices)
  </action>
  <verify>Create quotation. View in list. Edit status to accepted. Convert to invoice — new invoice created with same data.</verify>
  <done>Quotation CRUD with shared components, status workflow, convert-to-invoice action.</done>
</task>

<task type="auto">
  <name>Task 2: Convert-to-invoice API and quotation PDF</name>
  <files>src/app/api/quotations/[id]/convert/route.ts, src/components/documents/QuotationPdf.tsx</files>
  <action>
    Create `src/app/api/quotations/[id]/convert/route.ts`:
    - POST handler: authenticated
    - Fetches quotation with items
    - Creates new invoice with same client, line items, tax, discount
    - Sets quotation.converted_invoice_id to new invoice id
    - Returns new invoice id

    Create `src/components/documents/QuotationPdf.tsx`:
    - Same structure as InvoicePdf but with "QUOTATION" header
    - Shows expiry_date instead of due_date
    - Different document number format (QUO-YYYY-NNNN)
  </action>
  <verify>Call convert endpoint — new invoice created. Export quotation PDF — correct format with quotation branding.</verify>
  <done>Convert-to-invoice endpoint works. Quotation PDF exports correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Quotation management with CRUD, PDF export, convert-to-invoice</what-built>
  <how-to-verify>
    1. Go to /quotations — see list
    2. Create quotation with client and line items
    3. Change status to "Accepted"
    4. Click "Convert to Invoice" — redirects to new invoice with same data
    5. Export quotation PDF — branded PDF downloads
    6. Verify original quotation shows link to converted invoice
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Quotation pages mirror invoice structure
- Convert-to-invoice creates proper invoice
- Quotation PDF generates correctly
- Status workflow works
</verification>

<success_criteria>
Quotation management fully working with conversion to invoice, PDF export, shared components with invoices.
</success_criteria>

<output>
After completion, create `.planning/phases/06-quotations/06-01-SUMMARY.md`
</output>

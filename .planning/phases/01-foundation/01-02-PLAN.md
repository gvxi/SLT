---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - next-i18next.config.js
  - src/lib/i18n.ts
  - public/locales/en/common.json
  - public/locales/ar/common.json
  - src/app/layout.tsx
  - src/store/uiStore.ts
autonomous: true
must_haves:
  truths:
    - "App renders English text from translation files"
    - "Language can be toggled to Arabic"
    - "When Arabic is active, dir=rtl is set on html element"
    - "Font switches to Cairo for Arabic content"
  artifacts:
    - path: "public/locales/en/common.json"
      provides: "English translation strings"
    - path: "public/locales/ar/common.json"
      provides: "Arabic translation strings"
    - path: "src/store/uiStore.ts"
      provides: "Zustand store for UI state (language, theme, sidebar)"
    - path: "src/lib/i18n.ts"
      provides: "i18n configuration"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/store/uiStore.ts"
      via: "useUIStore hook for language"
      pattern: "useUIStore"
    - from: "src/app/layout.tsx"
      to: "src/theme/rtlCache.ts"
      via: "CacheProvider with direction-aware cache"
      pattern: "createEmotionCache"
---

<objective>
Set up i18n (Arabic/English) with RTL/LTR switching and Zustand UI state store.

Purpose: Enable bilingual support — the core UX requirement for this app.
Output: Working language toggle that switches text, direction, and font.
</objective>

<execution_context>
@C:/Users/SAI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/SAI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@AGENT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Zustand UI store and i18n configuration</name>
  <files>src/store/uiStore.ts, src/lib/i18n.ts, public/locales/en/common.json, public/locales/ar/common.json</files>
  <action>
    Create `src/store/uiStore.ts` with Zustand:
    - State: `language: 'en' | 'ar'`, `themeMode: 'light' | 'dark'`, `sidebarOpen: boolean`
    - Actions: `setLanguage(lang)`, `toggleTheme()`, `toggleSidebar()`, `setSidebarOpen(open)`
    - Persist language and themeMode to localStorage via zustand/middleware persist

    Create `src/lib/i18n.ts`:
    - Configure i18next with react-i18next
    - Use HttpBackend to load from /locales/{lang}/common.json
    - Default language: 'en', fallback: 'en'
    - Supported: ['en', 'ar']
    - Since this is App Router, use client-side i18n initialization (not next-i18next server-side)

    Create `public/locales/en/common.json` with initial keys:
    - nav.dashboard, nav.tasks, nav.products, nav.invoices, nav.quotations, nav.settings
    - common.search, common.save, common.cancel, common.delete, common.edit, common.create, common.loading
    - auth.login, auth.register, auth.email, auth.password, auth.logout

    Create `public/locales/ar/common.json` with Arabic translations for all the same keys.
  </action>
  <verify>Import uiStore in a test component — language toggles and persists to localStorage. Translation files parse as valid JSON.</verify>
  <done>Zustand store manages language/theme/sidebar state. i18n loads translations from JSON files. Both EN and AR translation files exist with nav, common, and auth keys.</done>
</task>

<task type="auto">
  <name>Task 2: Wire i18n and RTL into root layout</name>
  <files>src/app/layout.tsx, src/components/providers/AppProviders.tsx</files>
  <action>
    Create `src/components/providers/AppProviders.tsx` as a 'use client' component that:
    - Reads language from useUIStore
    - Initializes i18n on mount
    - Creates direction-aware Emotion cache via createEmotionCache(direction)
    - Creates direction-aware MUI theme via createTheme(direction)
    - Wraps children in: CacheProvider > ThemeProvider > CssBaseline > QueryClientProvider > children
    - Sets document.dir and document.lang when language changes

    Update `src/app/layout.tsx`:
    - Keep as server component
    - Wrap {children} in AppProviders
    - Import Inter and Cairo fonts from next/font/google, pass as CSS variables
  </action>
  <verify>Run `npm run dev`. Page loads in English (LTR). Add a temporary language toggle button — clicking it switches to Arabic (RTL), direction flips, font changes to Cairo.</verify>
  <done>Root layout dynamically switches between LTR/RTL based on language store. Emotion cache regenerates for RTL. MUI components respect direction. Font toggles between Inter and Cairo.</done>
</task>

</tasks>

<verification>
- `npm run dev` starts cleanly
- English content renders with Inter font, dir=ltr
- Arabic content renders with Cairo font, dir=rtl
- Language preference persists across page refresh (localStorage)
- MUI components (buttons, inputs) flip correctly in RTL
</verification>

<success_criteria>
Bilingual app with working RTL/LTR toggle, persisted language preference, correct fonts per language, and MUI components that respect direction.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>

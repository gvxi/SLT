---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/components/layout/Sidebar.tsx
  - src/components/layout/TopAppBar.tsx
  - src/components/layout/BottomNav.tsx
  - src/components/layout/PageWrapper.tsx
  - src/components/shared/LangToggle.tsx
  - src/app/dashboard/page.tsx
  - src/app/dashboard/layout.tsx
  - open-next.config.ts
  - wrangler.jsonc
autonomous: false
must_haves:
  truths:
    - "Desktop shows sidebar (240px) + top appbar + content area"
    - "Mobile (<900px) shows top appbar + content + bottom navigation"
    - "Sidebar collapses on desktop"
    - "Language toggle switches between AR and EN with visual feedback"
    - "Navigation links route to correct pages"
  artifacts:
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Desktop sidebar with nav items and collapse"
    - path: "src/components/layout/TopAppBar.tsx"
      provides: "Top app bar with search, avatar, hamburger on mobile"
    - path: "src/components/layout/BottomNav.tsx"
      provides: "Mobile bottom navigation bar"
    - path: "src/components/layout/PageWrapper.tsx"
      provides: "Page wrapper that composes sidebar + appbar + content"
  key_links:
    - from: "src/components/layout/PageWrapper.tsx"
      to: "src/components/layout/Sidebar.tsx"
      via: "Responsive composition"
      pattern: "Sidebar|BottomNav"
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/store/uiStore.ts"
      via: "sidebarOpen state"
      pattern: "useUIStore"
---

<objective>
Build the responsive layout shell (Sidebar, TopAppBar, BottomNav) and configure Cloudflare deployment.

Purpose: Establish the app's navigation structure and deploy-readiness.
Output: Fully responsive layout matching AGENT.md specs, Cloudflare config ready.
</objective>

<execution_context>
@C:/Users/SAI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/SAI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@AGENT.md
@CLOUDFLARE_SETUP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build responsive layout components</name>
  <files>src/components/layout/Sidebar.tsx, src/components/layout/TopAppBar.tsx, src/components/layout/BottomNav.tsx, src/components/layout/PageWrapper.tsx, src/components/shared/LangToggle.tsx</files>
  <action>
    All components are 'use client'.

    Create `src/components/layout/Sidebar.tsx`:
    - 240px width, collapsible (icon-only mode ~64px) via uiStore.sidebarOpen
    - Nav items: Dashboard, Tasks, Products, Invoices, Quotations, Settings — each with MUI icon + label
    - Use Next.js Link for routing, highlight active route using usePathname()
    - All labels use t('nav.xxx') — no hardcoded text
    - Hidden on mobile (<900px via MUI useMediaQuery)
    - Use MUI Drawer component (permanent variant on desktop)

    Create `src/components/layout/TopAppBar.tsx`:
    - MUI AppBar at top
    - Left: hamburger icon (mobile: opens Drawer, desktop: toggles sidebar collapse)
    - Center/left: page title (from route)
    - Right: LangToggle component + avatar/profile button
    - Respects RTL layout

    Create `src/components/layout/BottomNav.tsx`:
    - MUI BottomNavigation, visible only on mobile (<900px)
    - 4 items: Home (dashboard), Tasks, Products, Docs (invoices)
    - Uses usePathname for active state

    Create `src/components/shared/LangToggle.tsx`:
    - Toggle button: "EN | AR" using MUI ToggleButtonGroup or IconButton
    - Calls uiStore.setLanguage and changes i18n language

    Create `src/components/layout/PageWrapper.tsx`:
    - Composes Sidebar + TopAppBar + BottomNav + main content area
    - On desktop: sidebar left, appbar top, content fills remaining space
    - On mobile: appbar top, content fills, bottom nav fixed at bottom
    - Export as the main layout wrapper used in app/dashboard/layout.tsx

    Create `src/app/dashboard/layout.tsx`:
    - Wraps children in PageWrapper
    - This is the authenticated layout (all pages except /auth/* use this)

    Create `src/app/dashboard/page.tsx`:
    - Placeholder page with Typography: t('nav.dashboard') heading
    - Will be filled in Phase 07

    Create placeholder pages for all routes:
    - src/app/tasks/page.tsx, src/app/products/page.tsx, src/app/invoices/page.tsx, src/app/quotations/page.tsx, src/app/settings/page.tsx
    - Each just shows the page name as a heading using t()
  </action>
  <verify>Run `npm run dev`. Desktop: sidebar visible with all nav links, clicking navigates between pages. Resize to mobile: sidebar hidden, bottom nav appears. Language toggle switches direction.</verify>
  <done>Full responsive layout working: desktop sidebar + appbar, mobile appbar + bottom nav, language toggle, all route placeholders in place.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Cloudflare Workers deployment</name>
  <files>open-next.config.ts, wrangler.jsonc, .dev.vars, package.json, .gitignore, next.config.ts</files>
  <action>
    Follow CLOUDFLARE_SETUP.md exactly:
    1. Install @opennextjs/cloudflare and wrangler as devDependencies (if not already)
    2. Create open-next.config.ts with cloudflare-node wrapper
    3. Patch next.config.ts to add initOpenNextCloudflareForDev()
    4. Create wrangler.jsonc with app name from package.json
    5. Add preview/deploy/cf-typegen scripts to package.json
    6. Add .open-next/, cloudflare-env.d.ts, .wrangler/, .dev.vars to .gitignore
    7. Create .dev.vars with Supabase env var placeholders
    8. Run npm run cf-typegen to generate cloudflare-env.d.ts
    9. Remove any `export const runtime = "edge"` from source files
  </action>
  <verify>Run `npm run dev` — still works after Cloudflare config. `wrangler.jsonc` exists. `npm run preview` builds (may fail without env vars, that's expected).</verify>
  <done>Cloudflare deployment configured per CLOUDFLARE_SETUP.md. Preview and deploy scripts available in package.json.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete layout shell with responsive sidebar, top app bar, bottom nav (mobile), language toggle (EN/AR with RTL), and Cloudflare config</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Desktop (>900px): Verify sidebar (240px) with all 6 nav items, top app bar, collapsible sidebar
    3. Click each nav link — should navigate to placeholder pages
    4. Click language toggle — should switch to Arabic, page flips to RTL, font changes
    5. Resize browser to <900px — sidebar disappears, bottom nav appears with 4 items
    6. Verify wrangler.jsonc and open-next.config.ts exist at project root
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Desktop layout matches AGENT.md diagram (sidebar + appbar + content)
- Mobile layout matches AGENT.md diagram (appbar + content + bottom nav)
- Language toggle works bidirectionally
- All nav routes have placeholder pages
- Cloudflare config files present
</verification>

<success_criteria>
Responsive layout shell fully working with all navigation, bilingual toggle, and Cloudflare deployment config in place.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>

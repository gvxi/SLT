---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/theme/theme.ts
  - src/theme/rtlCache.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/types/index.ts
  - .env.local.example
autonomous: true
must_haves:
  truths:
    - "Next.js dev server starts without errors"
    - "MUI theme applies Deep Indigo primary color"
    - "Inter font loads for LTR content"
    - "TypeScript strict mode is enabled"
  artifacts:
    - path: "package.json"
      provides: "All dependencies installed"
    - path: "src/theme/theme.ts"
      provides: "MUI theme with Deep Indigo palette, Inter/Cairo fonts, 8px border radius"
    - path: "src/app/layout.tsx"
      provides: "Root layout with ThemeProvider"
    - path: "tsconfig.json"
      provides: "Strict TypeScript configuration"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/theme/theme.ts"
      via: "ThemeProvider import"
      pattern: "ThemeProvider.*theme"
---

<objective>
Initialize Next.js project with MUI v6 theme, TypeScript strict config, and core dependencies.

Purpose: Establish the foundational project with working dev server, theming, and type safety.
Output: Running Next.js app with MUI theme applied.
</objective>

<execution_context>
@C:/Users/SAI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/SAI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@AGENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project and install all dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, .env.local.example</files>
  <action>
    Run `npx create-next-app@latest . --typescript --app --src-dir --no-tailwind --eslint --import-alias "@/*"` (accept defaults, use src/ directory).

    Then install ALL project dependencies in one go:
    ```
    npm install @mui/material @mui/icons-material @emotion/react @emotion/cache @emotion/styled stylis-plugin-rtl @tanstack/react-query zustand react-hook-form @hookform/resolvers zod recharts @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities @react-pdf/renderer next-i18next react-i18next i18next @supabase/supabase-js
    ```

    Create `.env.local.example` with:
    ```
    NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
    NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
    SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
    ```

    Update `tsconfig.json` to ensure `strict: true` is set.
  </action>
  <verify>Run `npm run dev` — server starts on localhost:3000 without errors. Run `npx tsc --noEmit` — no type errors.</verify>
  <done>Next.js project created with all dependencies, TypeScript strict mode, .env.local.example present.</done>
</task>

<task type="auto">
  <name>Task 2: Create MUI theme, RTL cache, and root layout with ThemeProvider</name>
  <files>src/theme/theme.ts, src/theme/rtlCache.ts, src/app/layout.tsx, src/app/page.tsx, src/types/index.ts</files>
  <action>
    Create `src/theme/theme.ts`:
    - Light theme with Deep Indigo primary (#3F51B5)
    - Typography: Inter for LTR (import from next/font/google)
    - Border radius: 8px globally via shape.borderRadius
    - Comfortable density settings
    - Export both createLightTheme and createDarkTheme functions that accept direction ('ltr' | 'rtl')

    Create `src/theme/rtlCache.ts`:
    - Create Emotion cache with stylis-plugin-rtl for RTL support
    - Export createEmotionCache(direction) function

    Create `src/types/index.ts`:
    - Define core TypeScript interfaces from AGENT.md schema: Profile, Product, Task, TaskChecklist, Client, Invoice, InvoiceItem, Quotation, QuotationItem
    - All with proper types (UUID as string, dates as string, enums as union types)

    Update `src/app/layout.tsx`:
    - Import Inter and Cairo from next/font/google
    - Wrap children in MUI ThemeProvider + CssBaseline
    - Add Emotion CacheProvider
    - Set html lang="en" dir="ltr" as default
    - Add React Query QueryClientProvider

    Replace `src/app/page.tsx` with a simple redirect to /dashboard (use next/navigation redirect).
  </action>
  <verify>Run `npm run dev` — page loads with MUI styling applied (CssBaseline resets visible). Run `npx tsc --noEmit` — no type errors.</verify>
  <done>MUI theme with Deep Indigo active, RTL cache ready, root layout wired with ThemeProvider + QueryClientProvider, type definitions for all DB entities exist.</done>
</task>

</tasks>

<verification>
- `npm run dev` starts cleanly
- `npx tsc --noEmit` passes
- Browser shows MUI-styled page at localhost:3000
- package.json contains all required dependencies
</verification>

<success_criteria>
Working Next.js dev server with MUI Deep Indigo theme, TypeScript strict, all dependencies installed, core types defined.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

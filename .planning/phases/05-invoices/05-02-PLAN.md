---
phase: 05-invoices
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/documents/PdfTemplate.tsx
  - src/components/documents/InvoicePdf.tsx
  - src/lib/pdfExport.ts
  - src/app/invoices/[id]/page.tsx
autonomous: true
must_haves:
  truths:
    - "User can export invoice as PDF"
    - "PDF shows branded template with company info, line items, totals"
    - "PDF renders in correct language (EN or AR)"
  artifacts:
    - path: "src/components/documents/InvoicePdf.tsx"
      provides: "Invoice PDF template using @react-pdf/renderer"
    - path: "src/lib/pdfExport.ts"
      provides: "PDF generation utility"
  key_links:
    - from: "src/app/invoices/[id]/page.tsx"
      to: "src/components/documents/InvoicePdf.tsx"
      via: "Export PDF button"
      pattern: "InvoicePdf|pdf"
---

<objective>
Add PDF export for invoices using @react-pdf/renderer.

Purpose: Business-critical feature — clients need printable/downloadable invoices.
Output: PDF export button on invoice detail page.
</objective>

<execution_context>
@C:/Users/SAI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/SAI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@AGENT.md
@.planning/phases/05-invoices/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create invoice PDF template and export</name>
  <files>src/components/documents/PdfTemplate.tsx, src/components/documents/InvoicePdf.tsx, src/lib/pdfExport.ts</files>
  <action>
    Create `src/components/documents/PdfTemplate.tsx`:
    - Base PDF layout using @react-pdf/renderer (Document, Page, View, Text, StyleSheet)
    - Header: company logo placeholder, company name, address
    - Branded with Deep Indigo (#3F51B5) accents
    - Shared between invoice and quotation PDFs

    Create `src/components/documents/InvoicePdf.tsx`:
    - Uses PdfTemplate as wrapper
    - Sections: Invoice header (number, dates), Client info, Line items table, Totals (subtotal, tax, discount, grand total), Notes
    - Supports both LTR and RTL layout based on language prop
    - Register Cairo font for Arabic text

    Create `src/lib/pdfExport.ts`:
    - Export generateInvoicePdf(invoice, language) — returns Blob
    - Uses @react-pdf/renderer pdf() function to render to blob
    - Helper to trigger browser download

    Update `src/app/invoices/[id]/page.tsx`:
    - Add "Export PDF" button (MUI Button with download icon)
    - On click: generate PDF blob and trigger download
    - Loading state while generating
  </action>
  <verify>Open an invoice detail page. Click "Export PDF". PDF downloads with correct invoice data, line items, and totals.</verify>
  <done>Invoice PDF export works with branded template, line items, totals, bilingual support.</done>
</task>

</tasks>

<verification>
- PDF generates without errors
- PDF contains all invoice data (number, client, items, totals)
- Arabic PDF renders RTL with Cairo font
</verification>

<success_criteria>
Invoice PDF export generates branded, accurate documents in both languages.
</success_criteria>

<output>
After completion, create `.planning/phases/05-invoices/05-02-SUMMARY.md`
</output>

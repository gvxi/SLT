---
phase: 05-invoices
plan: 01
type: execute
wave: 1
depends_on: ["02-03", "04-01"]
files_modified:
  - src/app/invoices/page.tsx
  - src/app/invoices/new/page.tsx
  - src/app/invoices/[id]/page.tsx
  - src/components/documents/DocumentForm.tsx
  - src/components/documents/LineItemsTable.tsx
  - src/components/documents/StatusChip.tsx
  - src/components/documents/ClientSelect.tsx
autonomous: false
must_haves:
  truths:
    - "Invoice list shows all invoices with status filters"
    - "User can create invoice with line items linked to products"
    - "Invoice number is auto-generated"
    - "Tax and discount calculate correctly"
    - "Invoice status can be changed through workflow"
  artifacts:
    - path: "src/components/documents/DocumentForm.tsx"
      provides: "Shared invoice/quotation form with line items"
    - path: "src/components/documents/LineItemsTable.tsx"
      provides: "Editable line items table"
  key_links:
    - from: "src/components/documents/DocumentForm.tsx"
      to: "src/hooks/useInvoices.ts"
      via: "useCreateInvoice/useUpdateInvoice"
      pattern: "useCreateInvoice|useUpdateInvoice"
    - from: "src/components/documents/LineItemsTable.tsx"
      to: "src/hooks/useProducts.ts"
      via: "Product selection for line items"
      pattern: "useProducts"
---

<objective>
Build invoice management: list, create/edit form with line items, status workflow, calculations.

Purpose: Core business document — revenue tracking and client billing.
Output: Working /invoices pages with full CRUD and financial calculations.
</objective>

<execution_context>
@C:/Users/SAI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/SAI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@AGENT.md
@.planning/phases/02-database-auth/02-02-SUMMARY.md
@.planning/phases/04-product-catalog/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build invoice list and shared document components</name>
  <files>src/app/invoices/page.tsx, src/components/documents/StatusChip.tsx, src/components/documents/ClientSelect.tsx</files>
  <action>
    Create `src/components/documents/StatusChip.tsx`:
    - Reusable MUI Chip that color-codes document statuses
    - Invoice statuses: draft=grey, sent=blue, paid=green, overdue=red, cancelled=grey
    - Quotation statuses: draft=grey, sent=blue, accepted=green, rejected=red, expired=grey

    Create `src/components/documents/ClientSelect.tsx`:
    - Autocomplete component for selecting existing client or creating new inline
    - Uses useClients() hook
    - "Add new client" option at bottom opens inline form

    Create `src/app/invoices/page.tsx` ('use client'):
    - MUI Table listing invoices: invoice_number, client name, issue_date, due_date, total (calculated), status (StatusChip)
    - Filter tabs at top: All, Draft, Sent, Paid, Overdue
    - Search by invoice number or client name
    - "New Invoice" button → navigates to /invoices/new
    - Click row → navigates to /invoices/[id]
  </action>
  <verify>Navigate to /invoices. Status filter tabs work. StatusChip renders correct colors.</verify>
  <done>Invoice list with status filtering, search, and shared document components.</done>
</task>

<task type="auto">
  <name>Task 2: Build document form with line items, create/edit invoice</name>
  <files>src/components/documents/DocumentForm.tsx, src/components/documents/LineItemsTable.tsx, src/app/invoices/new/page.tsx, src/app/invoices/[id]/page.tsx</files>
  <action>
    Create `src/components/documents/LineItemsTable.tsx` ('use client'):
    - Editable table of line items: product (autocomplete from useProducts), description, qty, unit_price, line total (calculated)
    - Add row button, delete row button per row
    - When product selected, auto-fill description and unit_price from product data
    - Footer row: subtotal, tax amount (from tax_pct), discount, grand total
    - All calculations client-side, real-time as values change

    Create `src/components/documents/DocumentForm.tsx` ('use client'):
    - Reusable form for both invoices and quotations (prop: type = 'invoice' | 'quotation')
    - React Hook Form + Zod
    - Sections:
      1. Client selection (ClientSelect)
      2. Dates: issue_date (today default), due_date/expiry_date
      3. Line items (LineItemsTable)
      4. Tax % input, Discount input
      5. Notes EN, Notes AR (optional textareas)
      6. Status selector (only for edit mode)
    - Total calculation: subtotal - discount + (subtotal * tax_pct / 100)

    Create `src/app/invoices/new/page.tsx`:
    - Uses DocumentForm in create mode (type='invoice')
    - On submit: useCreateInvoice() with line items
    - On success: redirect to /invoices/[id]

    Create `src/app/invoices/[id]/page.tsx`:
    - Uses DocumentForm in edit mode
    - Pre-fills from useInvoice(id) with invoice_items
    - Status change actions (e.g., "Mark as Sent", "Mark as Paid")
    - Duplicate invoice action (copies data to new invoice)
    - Delete with confirmation
  </action>
  <verify>Create new invoice with 2+ line items. Totals calculate correctly. Save and view. Edit and change status. Duplicate creates new invoice.</verify>
  <done>Invoice CRUD with line items, auto-numbering, financial calculations, status workflow, duplication.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Invoice management: list with filters, create/edit with line items, calculations, status workflow</what-built>
  <how-to-verify>
    1. Go to /invoices — see invoice list (may be empty)
    2. Click "New Invoice" — create with client, 2 line items (select products), tax 15%
    3. Verify totals calculate: subtotal, tax, discount, grand total
    4. Save — invoice number auto-generated (INV-2026-0001)
    5. Edit invoice — change status to "Sent"
    6. Duplicate invoice — new invoice created with same data
    7. Switch to Arabic — all labels and layout translate correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Invoice list with status filter tabs
- Create invoice with line items from products
- Financial calculations accurate
- Auto-generated invoice numbers
- Status workflow transitions
</verification>

<success_criteria>
Complete invoice management with list, CRUD, line items, financial calculations, auto-numbering, status workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/05-invoices/05-01-SUMMARY.md`
</output>

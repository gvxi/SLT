---
phase: 02-database-auth
plan: 03
type: execute
wave: 3
depends_on: ["02-02", "01-03"]
files_modified:
  - src/app/auth/login/page.tsx
  - src/app/auth/register/page.tsx
  - src/app/auth/layout.tsx
  - src/middleware.ts
autonomous: false
must_haves:
  truths:
    - "User can register with email and password"
    - "User can login with email and password"
    - "Unauthenticated users are redirected to /auth/login"
    - "Authenticated users accessing /auth/* are redirected to /dashboard"
    - "Logout clears session and redirects to /auth/login"
  artifacts:
    - path: "src/app/auth/login/page.tsx"
      provides: "Login page with email/password form"
    - path: "src/app/auth/register/page.tsx"
      provides: "Registration page"
    - path: "src/middleware.ts"
      provides: "Next.js middleware for auth protection"
  key_links:
    - from: "src/app/auth/login/page.tsx"
      to: "src/hooks/useAuth.ts"
      via: "useLogin mutation"
      pattern: "useLogin"
    - from: "src/middleware.ts"
      to: "src/lib/supabase/client.ts"
      via: "Session check"
      pattern: "supabase.*auth"
---

<objective>
Build auth pages (login/register) and route protection middleware.

Purpose: Secure the app — only authenticated users access the main features.
Output: Working login/register flow with route protection.
</objective>

<execution_context>
@C:/Users/SAI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/SAI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@AGENT.md
@.planning/phases/02-database-auth/02-02-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build auth pages and middleware</name>
  <files>src/app/auth/login/page.tsx, src/app/auth/register/page.tsx, src/app/auth/layout.tsx, src/middleware.ts</files>
  <action>
    Create `src/app/auth/layout.tsx`:
    - Centered layout (no sidebar/appbar) — just a centered Card on a subtle background
    - AppProviders wrapper for theme/i18n
    - Language toggle in top-right corner

    Create `src/app/auth/login/page.tsx` ('use client'):
    - MUI Card with: email input, password input, login button, link to register
    - React Hook Form + Zod validation (email required + valid format, password required + min 6 chars)
    - On submit: call useLogin() from useAuth hook
    - On success: router.push('/dashboard')
    - On error: show MUI Alert with error message
    - All text via t() (auth.login, auth.email, auth.password, etc.)
    - Loading state on submit button

    Create `src/app/auth/register/page.tsx` ('use client'):
    - Same structure as login but with: full_name, email, password, confirm password
    - Zod: passwords must match, name min 2 chars
    - On submit: call useRegister(), then show success message (check email)
    - Link to login page

    Create `src/middleware.ts`:
    - Use Supabase auth helper to check session from cookies
    - Protected routes: everything except /auth/*, /api/*, /_next/*, /locales/*, favicon
    - If no session on protected route → redirect to /auth/login
    - If session on /auth/* → redirect to /dashboard
    - Use @supabase/ssr for cookie-based session handling in middleware
  </action>
  <verify>Run `npm run dev`. Visit /dashboard without login → redirected to /auth/login. Register a new user. Login with credentials → redirected to /dashboard. Visit /auth/login while logged in → redirected to /dashboard.</verify>
  <done>Auth flow works end-to-end: register, login, logout, protected routes, redirects.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete auth flow with login/register pages, route protection, and Supabase integration</what-built>
  <how-to-verify>
    1. Make sure .env.local has your Supabase credentials (URL, anon key, service role key)
    2. Run `npm run dev` and go to http://localhost:3000
    3. Should redirect to /auth/login
    4. Click "Register" link, create an account
    5. Login with the new credentials
    6. Should redirect to /dashboard with the sidebar layout
    7. Try accessing /auth/login while logged in — should redirect back to /dashboard
    8. Test language toggle on auth pages — should switch to Arabic
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Register creates user in Supabase Auth
- Login returns valid session
- Middleware protects routes correctly
- Auth pages are bilingual (EN/AR)
</verification>

<success_criteria>
End-to-end auth working: register, login, protected routes, session persistence, logout.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-auth/02-03-SUMMARY.md`
</output>

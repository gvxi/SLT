---
phase: 03-task-management
plan: 01
type: execute
wave: 1
depends_on: ["02-03"]
files_modified:
  - src/app/tasks/page.tsx
  - src/app/tasks/[id]/page.tsx
  - src/components/tasks/KanbanBoard.tsx
  - src/components/tasks/KanbanColumn.tsx
  - src/components/tasks/TaskCard.tsx
  - src/components/tasks/TaskDrawer.tsx
  - src/components/tasks/TaskFilters.tsx
autonomous: false
must_haves:
  truths:
    - "Tasks display in Kanban columns grouped by status"
    - "Dragging a task between columns updates its status"
    - "User can create a new task"
    - "User can view and edit task details in a drawer"
    - "Tasks can be filtered by status, priority, assignee"
  artifacts:
    - path: "src/components/tasks/KanbanBoard.tsx"
      provides: "Kanban board with @dnd-kit drag-and-drop"
    - path: "src/components/tasks/TaskDrawer.tsx"
      provides: "Task detail/edit drawer with checklist"
  key_links:
    - from: "src/components/tasks/KanbanBoard.tsx"
      to: "src/hooks/useTasks.ts"
      via: "useTasks hook for data, useUpdateTask for drag"
      pattern: "useTasks|useUpdateTask"
---

<objective>
Build the complete task management module: Kanban board with drag-and-drop, task CRUD, detail drawer, filters.

Purpose: Core productivity feature — task tracking with visual Kanban workflow.
Output: Fully working /tasks page with Kanban board.
</objective>

<execution_context>
@C:/Users/SAI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/SAI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@AGENT.md
@.planning/phases/02-database-auth/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Kanban board with drag-and-drop</name>
  <files>src/app/tasks/page.tsx, src/components/tasks/KanbanBoard.tsx, src/components/tasks/KanbanColumn.tsx, src/components/tasks/TaskCard.tsx, src/components/tasks/TaskFilters.tsx</files>
  <action>
    Create `src/components/tasks/KanbanBoard.tsx` ('use client'):
    - Use @dnd-kit/core DndContext + @dnd-kit/sortable for drag-and-drop
    - 4 columns: Backlog, In Progress, Review, Done
    - Fetch tasks via useTasks() hook
    - On drag end: optimistically update local state, then call useUpdateTask to persist status change
    - Horizontal scroll on mobile for columns

    Create `src/components/tasks/KanbanColumn.tsx`:
    - MUI Paper with grey.50 background
    - Column header: translated status name + Chip with count
    - Droppable area using @dnd-kit/sortable SortableContext
    - "Add task" button at bottom of each column

    Create `src/components/tasks/TaskCard.tsx`:
    - MUI Paper card with: title, priority badge (color-coded Chip), due date, assignee avatar
    - Draggable via @dnd-kit/sortable useSortable
    - Click opens TaskDrawer
    - Cursor: grab/grabbing states

    Create `src/components/tasks/TaskFilters.tsx`:
    - Filter bar above Kanban: status multi-select, priority select, assignee select, date range
    - Filters update URL query params (useSearchParams)

    Update `src/app/tasks/page.tsx`:
    - Compose TaskFilters + KanbanBoard
    - Page title via t('nav.tasks')
    - "New Task" button in top-right
  </action>
  <verify>Navigate to /tasks. Kanban columns render. Create a task via the API (or add button). Drag task between columns — status updates.</verify>
  <done>Kanban board displays tasks by status, drag-and-drop changes status with optimistic updates, filters work.</done>
</task>

<task type="auto">
  <name>Task 2: Build task detail drawer with checklist and edit</name>
  <files>src/components/tasks/TaskDrawer.tsx, src/app/tasks/[id]/page.tsx</files>
  <action>
    Create `src/components/tasks/TaskDrawer.tsx` ('use client'):
    - MUI Drawer (right side, ~480px on desktop, full width on mobile)
    - React Hook Form + Zod for task editing
    - Fields: title, description (multiline), status (select), priority (select), assignee (select from profiles), due date (DatePicker), linked product (select)
    - Checklist section: list of task_checklists with checkboxes, add new item, delete item
    - Save button calls useUpdateTask()
    - Delete button with confirmation dialog calls useDeleteTask()
    - All labels via t()

    Create `src/app/tasks/[id]/page.tsx`:
    - Full page view of a single task (same form as drawer but full-page)
    - Useful for direct navigation / sharing task URLs
    - Uses useTask(id) hook
  </action>
  <verify>Click a task card — drawer opens with task details. Edit title and save — persists. Add checklist item — appears. Toggle checklist done — updates.</verify>
  <done>Task detail drawer works with edit, checklist management, delete. Task detail page accessible via URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete task management: Kanban board with drag-and-drop, task CRUD, detail drawer with checklists, filters</what-built>
  <how-to-verify>
    1. Go to /tasks — see 4 Kanban columns
    2. Create a new task — appears in Backlog
    3. Drag task to "In Progress" — status updates
    4. Click task card — drawer opens with details
    5. Edit task title and save
    6. Add a checklist item, toggle it done
    7. Filter by priority — board updates
    8. Switch to Arabic — all labels translate, layout flips RTL
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Kanban board renders 4 columns
- Drag-and-drop updates status optimistically
- Task CRUD operations persist to Supabase
- Checklist items work
- Filters narrow results
- RTL layout works
</verification>

<success_criteria>
Full task management with Kanban drag-and-drop, CRUD, checklists, filters, bilingual support.
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-management/03-01-SUMMARY.md`
</output>

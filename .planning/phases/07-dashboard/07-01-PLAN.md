---
phase: 07-dashboard
plan: 01
type: execute
wave: 1
depends_on: ["03-01", "04-01", "05-01"]
files_modified:
  - src/app/dashboard/page.tsx
  - src/components/dashboard/KpiCard.tsx
  - src/components/dashboard/TaskStatusChart.tsx
  - src/components/dashboard/RevenueChart.tsx
  - src/components/dashboard/RecentActivity.tsx
  - src/hooks/useDashboard.ts
autonomous: false
must_haves:
  truths:
    - "Dashboard shows 4 KPI cards with real data"
    - "Bar chart shows tasks by status"
    - "Line chart shows monthly revenue"
    - "Recent activity feed shows latest actions"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Dashboard page with KPIs, charts, activity"
    - path: "src/components/dashboard/KpiCard.tsx"
      provides: "Reusable KPI card component"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/hooks/useDashboard.ts"
      via: "useDashboard hook"
      pattern: "useDashboard"
    - from: "src/hooks/useDashboard.ts"
      to: "src/app/api/dashboard/route.ts"
      via: "fetch /api/dashboard"
      pattern: "api/dashboard"
---

<objective>
Build the dashboard with KPI cards, charts, and recent activity.

Purpose: Overview page — the first thing users see after login.
Output: Data-rich dashboard with real-time metrics.
</objective>

<execution_context>
@C:/Users/SAI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/SAI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@AGENT.md
@.planning/phases/02-database-auth/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dashboard KPI cards and charts</name>
  <files>src/app/dashboard/page.tsx, src/components/dashboard/KpiCard.tsx, src/components/dashboard/TaskStatusChart.tsx, src/components/dashboard/RevenueChart.tsx, src/components/dashboard/RecentActivity.tsx, src/hooks/useDashboard.ts</files>
  <action>
    Create `src/hooks/useDashboard.ts`:
    - useDashboardStats() — useQuery calling GET /api/dashboard
    - Returns: openTasks, totalProducts, unpaidInvoices, pendingQuotations, tasksByStatus[], monthlyRevenue[], recentActivity[]

    Create `src/components/dashboard/KpiCard.tsx`:
    - MUI Card: large metric number (Typography h4, primary color), label below (body2, text.secondary), optional trend indicator
    - Props: title (via t()), value (number), icon (MUI icon), color (optional)

    Create `src/components/dashboard/TaskStatusChart.tsx`:
    - Recharts BarChart: X-axis = status (Backlog, In Progress, Review, Done), Y-axis = count
    - Wrapped in MUI Paper with title
    - ResponsiveContainer for resize
    - Bars colored per status

    Create `src/components/dashboard/RevenueChart.tsx`:
    - Recharts LineChart: X-axis = months, Y-axis = revenue from paid invoices
    - MUI Paper wrapper with title
    - Tooltip showing formatted currency

    Create `src/components/dashboard/RecentActivity.tsx`:
    - MUI List showing last 10 actions (task created, invoice paid, product added, etc.)
    - Each item: icon, description, timestamp (relative: "2 hours ago")
    - Uses data from dashboard API

    Update `src/app/dashboard/page.tsx`:
    - Grid layout: 4 KPI cards in top row (responsive: 4 cols on lg, 2 on md, 1 on sm)
    - Second row: TaskStatusChart (left) + RevenueChart (right)
    - Third row: RecentActivity (full width)
    - Uses useDashboardStats() hook
    - Loading skeleton states while data loads
  </action>
  <verify>Navigate to /dashboard. KPI cards show counts (may be 0). Charts render (may be empty). Layout is responsive.</verify>
  <done>Dashboard with 4 KPIs, 2 charts, activity feed, responsive grid, loading states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dashboard with KPI cards, bar chart (tasks), line chart (revenue), activity feed</what-built>
  <how-to-verify>
    1. Go to /dashboard
    2. See 4 KPI cards (numbers may be 0 if no data yet)
    3. Create some tasks and invoices — refresh dashboard to see updated counts
    4. Mark an invoice as paid — revenue chart should update
    5. Resize to mobile — cards stack, charts resize
    6. Switch to Arabic — all labels translate
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Dashboard loads with real data from Supabase
- KPI numbers match actual record counts
- Charts render with proper data
- Responsive layout works
</verification>

<success_criteria>
Dashboard shows real-time business metrics, charts visualize trends, everything is bilingual and responsive.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard/07-01-SUMMARY.md`
</output>

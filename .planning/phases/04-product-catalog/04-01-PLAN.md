---
phase: 04-product-catalog
plan: 01
type: execute
wave: 1
depends_on: ["02-03"]
files_modified:
  - src/app/products/page.tsx
  - src/app/products/[id]/page.tsx
  - src/components/products/ProductTable.tsx
  - src/components/products/ProductGrid.tsx
  - src/components/products/ProductForm.tsx
autonomous: false
must_haves:
  truths:
    - "Products display in table and grid views"
    - "User can toggle between table and grid"
    - "User can create, edit, and delete products"
    - "Inline editing works for price and stock"
    - "Products show bilingual names (EN/AR)"
  artifacts:
    - path: "src/components/products/ProductTable.tsx"
      provides: "MUI DataGrid table with inline editing"
    - path: "src/components/products/ProductGrid.tsx"
      provides: "Card grid view for products"
    - path: "src/components/products/ProductForm.tsx"
      provides: "Product create/edit form"
  key_links:
    - from: "src/components/products/ProductTable.tsx"
      to: "src/hooks/useProducts.ts"
      via: "useProducts + useUpdateProduct"
      pattern: "useProducts|useUpdateProduct"
---

<objective>
Build the product catalog module: table/grid views, CRUD, inline editing, bilingual names.

Purpose: Product data feeds into invoices, quotations, and tasks.
Output: Fully working /products page.
</objective>

<execution_context>
@C:/Users/SAI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/SAI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@AGENT.md
@.planning/phases/02-database-auth/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build product list with table/grid views</name>
  <files>src/app/products/page.tsx, src/components/products/ProductTable.tsx, src/components/products/ProductGrid.tsx</files>
  <action>
    Create `src/components/products/ProductTable.tsx` ('use client'):
    - MUI Table (dense mode) with columns: SKU, Name (shows name_en or name_ar based on active language), Category, Unit Price (formatted currency), Stock Qty, Status (Chip: active=green, inactive=grey)
    - Inline editing for unit_price and stock_qty cells (double-click to edit, Enter to save via useUpdateProduct)
    - Sortable columns (client-side sorting)
    - Pagination

    Create `src/components/products/ProductGrid.tsx` ('use client'):
    - MUI Grid of Cards showing product info
    - Each card: product name, SKU, price, stock, status chip, edit/delete actions
    - Responsive: 3 cols on lg, 2 on md, 1 on sm

    Update `src/app/products/page.tsx`:
    - Toggle button group: Table view / Grid view
    - Search bar filtering by name/SKU
    - "New Product" button
    - Render ProductTable or ProductGrid based on toggle
  </action>
  <verify>Navigate to /products. Toggle between table and grid views. Search filters products.</verify>
  <done>Product list with table/grid toggle, inline editing, search, pagination.</done>
</task>

<task type="auto">
  <name>Task 2: Build product form and detail page</name>
  <files>src/components/products/ProductForm.tsx, src/app/products/[id]/page.tsx</files>
  <action>
    Create `src/components/products/ProductForm.tsx` ('use client'):
    - React Hook Form + Zod validation
    - Fields: SKU (text, required, unique), Name EN (required), Name AR (optional), Category (select/autocomplete), Unit Price (number, min 0), Stock Qty (integer, min 0), Status (active/inactive toggle)
    - Mode: create or edit (passed via props)
    - On submit: useCreateProduct() or useUpdateProduct()
    - Renders in a MUI Dialog

    Create `src/app/products/[id]/page.tsx`:
    - Full-page product detail/edit view
    - Uses useProduct(id)
    - Same form as ProductForm but full-page layout
    - Delete button with confirmation
  </action>
  <verify>Create new product via form. Edit product details. Delete a product. All persists to Supabase.</verify>
  <done>Product CRUD complete with form validation, bilingual name support, detail page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Product catalog: table/grid views, inline editing, CRUD, search, bilingual</what-built>
  <how-to-verify>
    1. Go to /products — see empty state or products
    2. Click "New Product" — create a product with EN and AR names
    3. Toggle to grid view — see product card
    4. Toggle to table view — inline edit price (double-click)
    5. Click product to see detail page
    6. Switch to Arabic — name column shows Arabic name
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Table and grid views render correctly
- Inline editing persists changes
- Product form validates all fields
- Bilingual names display based on active language
</verification>

<success_criteria>
Complete product catalog with dual view modes, inline editing, CRUD, search, bilingual support.
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-catalog/04-01-SUMMARY.md`
</output>
